<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Tab" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/celticBlue</item>
        <item name="colorPrimaryVariant">@color/spaceCadet</item>
        <item name="colorOnPrimary">@color/cultured</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/viridian</item>
        <item name="colorSecondaryVariant">@color/darkSpringGreen</item>
        <item name="colorOnSecondary">@color/cultured</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
<!--        <item name="android:colorBackground">@color/white</item>-->
    </style>

    <style name="Theme.Tab.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Tab.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Tab.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>